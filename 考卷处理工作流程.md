# 考卷处理工作流程与规范

> 本文件用于指导后续新增考卷时的处理流程、常见错误及验证清单。

---

## 一、处理新考卷的标准流程

### Step 1：导出PDF页面为图片

```bash
source /tmp/pdf_venv/bin/activate
python3 << 'PYEOF'
import fitz, os
from PIL import Image

doc = fitz.open("考卷路径.pdf")
name = "考卷简称"  # 如 "2604杭州滨江区一模"
outdir = f"/tmp/pdf_pages/{name}"
os.makedirs(outdir, exist_ok=True)

for i, page in enumerate(doc):
    pix = page.get_pixmap(dpi=120)
    img = Image.frombytes("RGB", [pix.width, pix.height], pix.samples)
    img.save(f"{outdir}/p{i+1}.jpg", quality=85)
doc.close()
PYEOF
```

### Step 2：OCR识别（使用OCR.space API）

```python
import requests, base64, time

API_KEY = "K81700788888957"  # 用户提供的key，500次/日

with open("图片路径.jpg", "rb") as f:
    img_b64 = base64.b64encode(f.read()).decode()

r = requests.post(
    "https://api.ocr.space/parse/image",
    data={
        "base64Image": f"data:image/jpeg;base64,{img_b64}",
        "language": "chs",
        "isOverlayRequired": "false",
        "OCREngine": "2",
    },
    headers={"apikey": API_KEY},
    timeout=60
)
result = r.json()
text = result["ParsedResults"][0]["ParsedText"]
```

> 注意：每页间加 `time.sleep(1.5)` 避免触发限速

### Step 3：逐题分析知识点

对每道题（判断题、选择题、综合题）识别其考查的知识点，对照现有资料判断是否已覆盖。

### Step 4：更新miss.md

将未覆盖的知识点按类别（历史/地理/道法）整理到 miss.md 的**表格末尾**（不要插入中间），并注明来源（哪份卷子哪道题）。

**重要规则**：
- 每条记录必须标注状态（✅ 已补充 / ❌ 仍缺失 / ⚠️ 部分补充）
- 新发现的缺失项统一**追加到表格末尾**
- 补充完成后立即将状态改为 `✅`
- 添加前先搜索表格中是否已有相同知识点，避免重复

### Step 5：补充知识点

- **独立新章节** → 新建目录和文件（知识点+填空题+材料题）
- **已有章节补充** → 打开对应 `.md` 文件，在合适位置插入内容

### Step 6：同步配套文件

知识点文件改完后，必须同步更新：
- ✅ `_填空题.md`
- ✅ `_材料题.md`（选择题和判断题考点可不加材料题）
- ✅ `_打印版.md`（重新生成）

### Step 7：更新README总索引

新增或修改章节后更新 [README.md](README.md)。

### Step 8：推送GitHub

```bash
git add -A
git commit -m "描述改动内容"
git push
```

---

## 二、今天犯过的错误（避免重犯）

### ❌ 错误1：填空题题号冲突

**场景**：在世界史_填空题.md和国情教育_填空题.md中新增题目时，新增的题号（10-13）与原文件已有的题号（10-13）冲突，导致有两个"第10题"。

**原因**：直接插入新题时没有检查原文件末尾是否已有相同题号的题目。

**预防**：
- 插入前先看原文件末尾的题号，新增题号从 `原最大号+1` 开始
- 用 `grep -n "^[0-9]" 文件.md | tail -5` 检查末尾题号

### ❌ 错误2：填空题重复内容

**场景**：国情教育_填空题.md中新增了"中国梦"的填空题，但原文末尾**已有的清单已有同一个考点**。

**原因**：对原文件内容记忆不准，新增了冗余题目。

**预防**：插入前先用 `grep "关键词"` 确认该考点是否已存在。

### ❌ 错误3：知识点补充了但配套练习没同步

**场景**：往秦汉.md补充了"西域都护"、往中国近现代史.md补充了"开罗宣言"等大量内容，但对应的 `_填空题.md` 和 `_材料题.md` 文件没有同步更新。

**后果**：知识点文档和练习题文档不一致，用户打印会遗漏新考点。

**预防**：**修改任何知识点 .md 文件后，立即同步更新配套文件，不要等最后一起补。**

### ❌ 错误4：新建章节材料题漏创建

**场景**：3个世界史新章节（古代亚非文明、古代欧洲文明、封建时代亚洲）创建时只做了知识点和填空题，材料题和打印版被遗漏。

**原因**：创建新章节时默认"有知识点就有填空题"但没把材料题纳入必需清单。

**预防**：新建章节的"四件套"必须一次完成：
1. 知识点 `.md` ✅
2. 填空题 `_填空题.md` ✅
3. 材料题 `_材料题.md` ✅
4. 打印版 `_打印版.md` ✅

### ❌ 错误5：打印版未及时同步（反复发生）

**场景**：知识点源文件修改了十几次，打印版只在最后集中更新了一次。期间用户打开打印版发现内容落后。

**原因**：打印版是知识点+填空题+材料题的拼接产物，源文件变了打印版不会自动变。

**预防**：知识点文件改完后**立刻**重新生成打印版（用写好的regenerate函数）。

### ❌ 错误6：中考高频考点表格缺失

**场景**：4份新建知识点文档的"中考高频考点"表格为空或不存在。

**原因**：新创建文件时模板不完整。

**预防**：创建新章节时直接复制夏商周.md的结构模板，确保每个部分都到位。

### ❌ 错误7：平权应用内容重复

**场景**：宪法与公民权利义务.md中，"平等权应用"内容被同时插入了"公民基本权利"和"公民基本义务"两个章节下。

**原因**：修改时定位不精确，覆盖区域过大。

**预防**：编辑时先 `grep` 确认唯一匹配位置；替换时缩小匹配范围。

### ❌ 错误8：PlantUML图过宽不适合A4打印

**场景**：WBS图多个横向分支（6-7个同级节点），打印到A4时被截断。

**预防**：WBS图的同级分支不超过**4个**，超过则拆分成多个小图。

---

## 三、新章节必须包含的四件套检查清单

| 文件 | 路径示例 | 必须包含 |
|------|---------|---------|
| `知识点.md` | `历史/世界史/古代亚非文明/古代亚非文明.md` | 考点总览表、核心知识、记忆口诀、中考高频考点表格、必背清单 |
| `_填空题.md` | `.../古代亚非文明_填空题.md` | 填空题目 + 参考答案 |
| `_材料题.md` | `.../古代亚非文明_材料题.md` | 通用答题模板 + 各专题出题角度 + 答题要点 |
| `_打印版.md` | `.../古代亚非文明_打印版.md` | 知识点 + 填空题 + 材料题 + 参考答案（顺序拼接） |

---

## 四、知识点文件改动后的强制验证清单

每改完一个知识点文件，逐项检查：

```markdown
□ 1. 知识点文件本身完整（考点表+核心知识+口诀+高频考点+必背清单）
□ 2. `_填空题.md` 已同步新增内容（无题号冲突、无重复）
□ 3. `_材料题.md` 已同步新增内容
□ 4. `_打印版.md` 已重新生成
□ 5. 若新增章节 → README.md已更新
```

---

## 五、打印版生成命令

```bash
cat 知识点.md > 打印版.md
echo -e "\n\n---\n\n# 📝 填空题\n\n---\n" >> 打印版.md
awk 'BEGIN{ref=0} /^## 参考答案/{ref=1; next} ref==0{print}' 填空题.md >> 打印版.md
[ -f 材料题.md ] && echo -e "\n\n---\n\n# 📝 材料题专项\n\n---\n" >> 打印版.md
[ -f 材料题.md ] && cat 材料题.md >> 打印版.md
echo -e "\n\n---\n\n## 参考答案\n" >> 打印版.md
awk 'BEGIN{found=0} /^## 参考答案/{found=1; next} found==1{print}' 填空题.md >> 打印版.md
```

---

## 六、OCR注意事项

1. **OCR.space免费额度**：`helloworld` 公共key限制**20次/小时**；注册后免费key限制**500次/天**
2. **限速处理**：每页请求间隔至少1.5秒；若触发限速需等待1小时
3. **识别质量**：扫描件OCR可能有错位、错字，需人工校对关键信息（人名、地名、数字、条约名称）
4. **备用方案**：若无网络，需要安装 `tesseract-ocr` + `tesseract-ocr-chi-sim`（需sudo权限）
